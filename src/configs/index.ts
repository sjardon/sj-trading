import AppConfig from './app.config';
import AuthConfig from './auth.config';
import DbConfig from './db.config';
import ExchangeConfig from './exchange.config';

export default [AppConfig, AuthConfig, DbConfig, ExchangeConfig];
